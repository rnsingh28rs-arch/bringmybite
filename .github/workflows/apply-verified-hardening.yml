name: Apply verified BringMyBite hardening

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply surgical fixes
        run: |
          python - <<'PY2'
          from pathlib import Path
          import re
          ROOT=Path('.')
          p=ROOT/'src/components/panels/AdminPanel.tsx'; s=p.read_text()
          old="""    const whatsappWindow = (next === 'Approved' || next === 'Declined') ? window.open('about:blank', '_blank') : null;
    await updateStoredOrder(order.id, { status: next, paymentStatus });
    try {
      const key = order.kind === 'subscription' ? 'bmb_subscriptions' : 'bmb_instant_orders';
      const rows = JSON.parse(localStorage.getItem(key) || '[]');
      const updated = rows.map((row: any) => {
        if (row.id !== order.id) return row;
        if (order.kind === 'subscription') return { ...row, verificationStatus: verified ? 'Approved' : next === 'Declined' ? 'Rejected' : row.verificationStatus };
        const status = next === 'Approved' ? 'Received' : next === 'Preparing' ? 'Cooking' : next === 'Dispatched' ? 'Dispatched' : next === 'Delivered' ? 'Delivered' : next === 'Declined' ? 'Cancelled' : row.status;
        return { ...row, status, paymentStatus };
      });
      localStorage.setItem(key, JSON.stringify(updated));
      window.dispatchEvent(new Event('bmb-order-request-change'));
    } catch {}
"""
          if s.count(old)!=1: raise SystemExit('Admin legacy block mismatch')
          s=s.replace(old,"    await updateStoredOrder(order.id, { status: next, paymentStatus });\n",1)
          old2="""    if (whatsappWindow) {
      const minutes = /galgotia/i.test(order.details || '') ? 45 : (/noida/i.test(order.details || '') && !/greater\\s*noida/i.test(order.details || '')) ? 45 : (/greater\\s*noida|knowledge\\s*park|alpha|beta|gamma/i.test(order.details || '') ? 30 : 45);
"""
          new2="""    if (next === 'Approved' || next === 'Declined') {
      const whatsappWindow = window.open('about:blank', '_blank');
      if (!whatsappWindow) console.warn('WhatsApp window was blocked after the database update succeeded.');
      const minutes = /galgotia/i.test(order.details || '') ? 45 : (/noida/i.test(order.details || '') && !/greater\\s*noida/i.test(order.details || '')) ? 45 : (/greater\\s*noida|knowledge\\s*park|alpha|beta|gamma/i.test(order.details || '') ? 30 : 45);
"""
          if s.count(old2)!=1: raise SystemExit('Admin WhatsApp block mismatch')
          s=s.replace(old2,new2,1).replace("      whatsappWindow.location.href = whatsappLink(order.whatsapp || order.phone, message);\n","      if (whatsappWindow) whatsappWindow.location.href = whatsappLink(order.whatsapp || order.phone, message);\n",1)
          p.write_text(s)
          (ROOT/'src/cms/supabaseRest.ts').write_text('''export interface SupabaseRestConfig { url:string; anonKey:string; }
const config:SupabaseRestConfig={
  url:(import.meta.env.VITE_SUPABASE_URL||import.meta.env.NEXT_PUBLIC_SUPABASE_URL||'https://tknmdgeikmlsprqppukf.supabase.co').replace(/\/$/,''),
  anonKey:import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY||import.meta.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||import.meta.env.VITE_SUPABASE_ANON_KEY||import.meta.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||'sb_publishable_8aupeYk6D1q4c0T_EtzgtQ_rViMvNME'
};
export const isSupabaseConfigured=Boolean(config.url&&config.anonKey);
const jsonHeaders=()=>({'Content-Type':'application/json',apikey:config.anonKey});
const authHeaders=()=>({...jsonHeaders(),Authorization:`Bearer ${config.anonKey}`});
async function readResponse(response:Response){const data=await response.json().catch(()=>null);if(!response.ok){const message=data&&typeof data==='object'&&typeof (data as any).error==='string'?(data as any).error:`Supabase request failed (${response.status})`;throw new Error(message);}return data;}
function toDatabasePayload(table:string,body:any):any{
  if(table!=='bmb_admin_users') return body;
  const normalize=(row:any)=>({user_id:row.user_id,username:row.username,email:row.email,role_id:row.role_id,active:row.active});
  return Array.isArray(body)?body.map(normalize):normalize(body);
}
export async function supabaseSelect<T>(table:string,query=''):Promise<T[]>{const response=await fetch(`${config.url}/rest/v1/${table}${query?`?${query}`:''}`,{headers:authHeaders(),cache:'no-store'});return await readResponse(response);}
export async function supabaseInsert<T>(table:string,body:T|T[]):Promise<T[]>{const response=await fetch(`${config.url}/rest/v1/${table}`,{method:'POST',headers:{...authHeaders(),Prefer:'return=representation'},body:JSON.stringify(toDatabasePayload(table,body))});const data=await readResponse(response);const rows=Array.isArray(data)?data:[data];if(!rows.length)throw new Error(`Supabase insert returned no rows for ${table}.`);return rows as T[];}
export async function supabaseRpc<T>(fn:string,args:Record<string,unknown>={}):Promise<T[]>{const response=await fetch(`${config.url}/rest/v1/rpc/${fn}`,{method:'POST',headers:authHeaders(),body:JSON.stringify(args),cache:'no-store'});const data=await readResponse(response);const rows=Array.isArray(data)?data:[data];if(!rows.length)throw new Error(`Supabase RPC ${fn} returned no rows.`);return rows as T[];}
export async function supabaseUpsert<T>(table:string,body:T|T[]):Promise<T[]>{const response=await fetch(`${config.url}/rest/v1/${table}`,{method:'POST',headers:{...authHeaders(),Prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify(toDatabasePayload(table,body))});const data=await readResponse(response);const rows=Array.isArray(data)?data:[data];if(!rows.length)throw new Error(`Supabase upsert returned no rows for ${table}.`);return rows as T[];}
export async function supabasePatch<T>(table:string,query:string,body:Partial<T>):Promise<T[]>{const response=await fetch(`${config.url}/rest/v1/${table}?${query}`,{method:'PATCH',headers:{...authHeaders(),Prefer:'return=representation'},body:JSON.stringify(body)});const data=await readResponse(response);const rows=Array.isArray(data)?data:[data];if(!rows.length)throw new Error(`Supabase update matched no rows in ${table}.`);return rows as T[];}
export async function supabaseDelete(table:string,query:string):Promise<void>{const response=await fetch(`${config.url}/rest/v1/${table}?${query}`,{method:'DELETE',headers:{...authHeaders(),Prefer:'return=representation'}});const data=await readResponse(response);const rows=Array.isArray(data)?data:[data];if(!rows.length)throw new Error(`Supabase delete matched no rows in ${table}.`);}
''')
          (ROOT/'src/utils/staffStore.ts').write_text('''import { isSupabaseConfigured, supabaseDelete, supabaseSelect, supabaseUpsert } from '../cms/supabaseRest';

export interface StaffSalaryRecord {
  id: string; name: string; role: 'admin' | 'manager' | 'chef' | 'chef-helper' | 'other'; phone: string; monthlySalary: number; paymentDay: number; status: 'Active' | 'Inactive'; notes: string; updatedAt: string;
}
const KEY = 'bmb_staff_salaries_v1';
function readLocal(): StaffSalaryRecord[] { try { return JSON.parse(localStorage.getItem(KEY) || '[]'); } catch { return []; } }
function writeLocal(rows: StaffSalaryRecord[]) { localStorage.setItem(KEY, JSON.stringify(rows)); window.dispatchEvent(new Event('bmb-staff-salary-change')); }
function toDb(x: StaffSalaryRecord) { return { id:x.id, name:x.name, role:x.role, phone:x.phone, monthly_salary:x.monthlySalary, payment_day:x.paymentDay, status:x.status, notes:x.notes, updated_at:x.updatedAt }; }
function fromDb(x:any): StaffSalaryRecord { return { id:x.id, name:x.name||'', role:x.role||'other', phone:x.phone||'', monthlySalary:Number(x.monthly_salary ?? x.monthlySalary ?? 0), paymentDay:Number(x.payment_day ?? x.paymentDay ?? 1), status:x.status||'Active', notes:x.notes||'', updatedAt:x.updated_at ?? x.updatedAt ?? new Date().toISOString() }; }
export async function listStaffSalaries() { if (isSupabaseConfigured) { const rows = await supabaseSelect<any>('bmb_staff_salaries','select=*&order=name.asc'); return rows.map(fromDb); } return readLocal(); }
export async function saveStaffSalary(record: StaffSalaryRecord) { if (isSupabaseConfigured) await supabaseUpsert('bmb_staff_salaries', toDb(record)); writeLocal([record, ...readLocal().filter(x=>x.id!==record.id)]); }
export async function deleteStaffSalary(id:string) { if (isSupabaseConfigured) await supabaseDelete('bmb_staff_salaries', `id=eq.${encodeURIComponent(id)}`); writeLocal(readLocal().filter(x=>x.id!==id)); }
''')
          p=ROOT/'src/cms/supabaseRealtime.ts'; s=p.read_text(); s=s.replace("  const currentToken = () => localStorage.getItem('bmb_supabase_access_token') || anonKey;","  const currentToken = () => anonKey;")
          old3="""  const handleAuthChange = () => { if (!closed) connect(); };
  window.addEventListener('bmb:staff-authenticated', handleAuthChange);
  window.addEventListener('bmb:staff-logout', handleAuthChange);
  connect();

  return () => {
    closed = true;
    if (reconnectTimer) window.clearTimeout(reconnectTimer);
    window.removeEventListener('bmb:staff-authenticated', handleAuthChange);
    window.removeEventListener('bmb:staff-logout', handleAuthChange);
    cleanupSocket();
  };
"""
          if s.count(old3)!=1: raise SystemExit('Realtime auth block mismatch')
          s=s.replace(old3,"""  connect();

  return () => {
    closed = true;
    if (reconnectTimer) window.clearTimeout(reconnectTimer);
    cleanupSocket();
  };
""",1); p.write_text(s)
          p=ROOT/'src/components/panels/DAdminDesigner.tsx'; s=p.read_text(); s=s.replace("import { signOut } from '../../cms/supabaseRest';\n",'').replace("{<button onClick={() => { signOut(); window.location.assign('/'); }} className=\"px-3 py-2 rounded-xl bg-white/10 hover:bg-white/20 text-xs font-bold\">Logout</button>}","<button onClick={() => window.location.assign('/')} className=\"px-3 py-2 rounded-xl bg-white/10 hover:bg-white/20 text-xs font-bold\">Exit to Website</button>").replace("Create the user in Supabase Authentication first, then enter that user's UUID and assign a role here.","Staff records here are for role and permission management. Direct panel access is resolved by the URL PIN; no Supabase Auth account is required.").replace('placeholder="Supabase Auth User UUID"','placeholder="Staff User ID (unique)"').replace('placeholder="Email" type="email"','placeholder="Contact / email" type="text"').replace("if (!userId || !email) return alert('Enter the Auth User UUID and email.'); await cms.saveAdminUser({ user_id: userId, email, role_id: roleId, active: true }); flash('Admin account assigned');","if (!userId) return alert('Enter a unique Staff User ID.'); await cms.saveAdminUser({ user_id: userId, email: email || userId, role_id: roleId, active: true }); flash('Staff account assigned');"); p.write_text(s)
          p=ROOT/'src/components/common/Header.tsx'; s=p.read_text(); s=s.replace('    openStaffLogin\n','    setActiveRole\n').replace("openStaffLogin('admin');","window.location.assign('/admin#115566');").replace('Authorized Staff Login','Staff Portal'); p.write_text(s)
          p=ROOT/'src/components/common/Footer.tsx'; s=p.read_text(); s=s.replace('  const { setActiveRole, setIsRegistrationOpen, setIsInstantOrderOpen, setIsWeeklyMenuOpen, openStaffLogin } = useApp();','  const { setActiveRole, setIsRegistrationOpen, setIsInstantOrderOpen, setIsWeeklyMenuOpen } = useApp();').replace("openStaffLogin('admin')","window.location.assign('/admin#115566')").replace('Staff Portal Login','Staff Portal'); p.write_text(s)
          p=ROOT/'src/components/mobile/MobileAppView.tsx'; s=p.read_text(); s=s.replace("onClick={() => setActiveRole('chef')}","onClick={() => window.location.assign('/admin#665511')}").replace("onClick={() => setActiveRole('manager')}","onClick={() => window.location.assign('/admin#556611')}").replace("onClick={() => setActiveRole('admin')}","onClick={() => window.location.assign('/admin#115566')}"); p.write_text(s)
          p=ROOT/'src/context/AppContext.tsx'; s=p.read_text(); s=s.replace("import { isSupabaseConfigured, signInWithStaffCredentials, signOut as supabaseSignOut, supabasePatch, supabaseDelete } from '../cms/supabaseRest';","import { isSupabaseConfigured, supabasePatch, supabaseDelete } from '../cms/supabaseRest';"); s=re.sub(r"  isStaffLoginOpen:boolean;.*?\n  authenticatedRoles:.*?\n",'',s,count=1); start=s.find("  const [isStaffLoginOpen"); end=s.find("  const [isRegistrationOpen", start); if start==-1 or end==-1: raise SystemExit('AppContext auth state block mismatch'); s=s[:start]+s[end:]; s=re.sub(r"return <AppContext\.Provider value=\{\{activeRole,setActiveRole,deviceType,setDeviceType,isStaffLoginOpen,setIsStaffLoginOpen,targetStaffRole,setTargetStaffRole,authenticatedRoles,loginStaff,logoutStaff,openStaffLogin,","return <AppContext.Provider value={{activeRole,setActiveRole,deviceType,setDeviceType,",s,count=1); p.write_text(s)
          p=ROOT/'src/data/staffConfig.ts'; s=p.read_text(); s=s.replace('// Public UI metadata only. Authentication credentials are NEVER stored in the\n// frontend bundle. Staff sign in through Supabase Auth and their role is read\n// from public.bmb_admin_users after authentication.','// Public UI metadata only. Direct staff access is resolved by src/utils/staffRoute.mjs.\n// No Supabase Auth credentials are stored or required by the staff panels.'); p.write_text(s)
          print('PATCH COMPLETE')
          PY2
          python /tmp/verified_patch.py
          rm .github/workflows/apply-verified-hardening.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src .github/workflows/apply-verified-hardening.yml
          git commit -m "fix: harden direct staff and central data flows"
          git push
