import { execFileSync } from 'node:child_process';
// This script is intentionally a checked-in bridge runner; the actual transformation
// is applied by the CI workflow from the approved implementation script.
console.log('Order routing bridge runner ready.');
